# 🧩 Backend API для учебных и практических материалов

## Тип проекта

**Backend-only** (REST API на чистом PHP 8.3)

---

## Назначение

Серверная часть учебного проекта, обеспечивающая управление и хранение учебных и практических материалов (работы, страницы, сайты и т.д.).
Используется совместно с React SPA (админ-панель) для централизованного редактирования **логических моделей ресурсов**, хранящихся в JSON-файлах.

JSON описывает **структуру и смысл ресурсов**, а сервер полностью управляет физическим хранением файлов и формированием публичных URL.

---

## Основной функционал

- CRUD-операции для всех JSON-файлов (кроме `config.json`)
- Загрузка PDF-файлов и ZIP-архивов
- Распаковка ZIP-архивов по **семантическим правилам ресурса**
- Автоматическая генерация `id` на стороне сервера
- Формирование публичных URL сервером
- Basic-аутентификация (пароль в виде хеша)
- Частичное обновление JSON-записей
- Валидация данных по JSON-схемам

---

## Технологический стек

| Компонент       | Описание                                  |
| --------------- | ----------------------------------------- |
| Язык            | PHP 8.3                                   |
| Формат данных   | JSON                                      |
| API             | REST                                      |
| Архитектура     | **Слоисто-модульная (Layered + Modular)** |
| Аутентификация  | Basic Auth (по хешу пароля)               |
| Хранение данных | `/data/*.json`                            |
| Валидация       | JSON Schema                               |
| Доступ          | Только из React Admin Panel               |

---
# Архитектура

## Ключевые архитектурные принципы

### 1️⃣ JSON = логическая модель ресурса

JSON-файлы **не содержат**:

- физических путей на сервере
- внутренних директорий (`/files`, `uploads`, и т.д.)
- технических деталей хранения

JSON **содержит**:

- `id` — идентификатор сущности (для API и связей)
- человекочитаемые имена (`name`, `fileName`)
- доменные поля (repositoryUrl, figmaUrl, referenceColumns и т.д.)
- публичные `url` и `references`, готовые для использования фронтом

---

### 2️⃣ `id` — только для API

- Генерируется сервером автоматически
- Используется только:
  - в API-эндпоинтах
  - для внутренних связей между сущностями
- **Не используется** для:
  - формирования папок
  - имён файлов
  - публичных URL

---

### 3️⃣ Физическое хранение — по смыслу ресурса

Физическая структура файлов полностью контролируется сервером и **не отражается напрямую в JSON**.

#### Сайты

` /files/sites/{site-slug}/`

- `site-slug` — логическое имя сайта
- не зависит от `id`
- не зависит от имени ZIP-архива

#### Практические страницы

`/files/pages/lab1/`
`/files/pages/lab2/`

- формируются по номеру лабораторной работы

#### PDF и прочие артефакты

`/files/labs/`
`/files/practices/`
`/files/courseworks/`

- формируются по типу работы

---

### 4️⃣ Имя файла берётся из загружаемого файла

- Пользователь **не передаёт имя файла отдельно**
- Сервер сохраняет имя файла **в исходном виде**, включая кириллицу
- Допускаются имена вида:
  - `ЛР1_Драганов_Д.А._823.pdf`
  - `Практика_2_Иванов.pdf`
- Сервер выполняет **только проверки безопасности**:
  - запрещённые символы
  - попытки directory traversal

В JSON сохраняется `fileName` — для отображения и генерации публичного URL.

---

### 5️⃣ Публичные URL формирует сервер

- Сервер сопоставляет логическую модель с физическим хранилищем
- Фронт получает **готовые URL** и не знает:
  - физических путей
  - `id`
  - внутренней структуры каталогов

---

## Концепция слоисто-модульной архитектуры

Архитектура **слоисто-модульная** — сочетает Layered и Modular подходы.

- **Слои**: Controller → Service → Repository → Utils
- **Модули**: изолированные бизнес-домены (works, sites, drafts и т.д.)

---

## Общая структура проекта

> 📝 **Примечание:** Структура была исправлена для ясности.

```
/
├── api/
│   ├── index.php              # Точка входа API
│   ├── core/                  # Общие компоненты
│   ├── modules/               # Модули бизнес-доменов
│   ├── config/                # Конфигурация
│   └── bootstrap.php          # Инициализация
├── data/                      # JSON-файлы с данными
├── files/                     # Загруженные файлы
└── assets/                    # Файлы фронтенда
```

---

## Принципы модульности

- Каждый модуль полностью автономен.
- Новые модули добавляются через `modules/<module-name>/`.
- Общие компоненты — в `/api/core/`.
- Все модули регистрируются в `bootstrap.php`.
---
# Спецификация API

## Общие сведения

- **Тип:** REST API
- **Базовый URL:** `/api/`
- **Формат:** JSON
- **Авторизация:** Basic Auth
- **Методы:** `GET`, `POST`, `PUT`, `DELETE`

---

## Основные ресурсы

Помимо загрузки файлов, API предоставляет стандартные CRUD-эндпоинты для всех основных моделей данных.

| Ресурс (Эндпоинт)         | JSON-файл                     | Описание                              |
| ------------------------- | ----------------------------- | ------------------------------------- |
| `/api/sites`              | `sites.json`                  | Управление сайтами                    |
| `/api/works`              | `labs.json`, `practices.json` | Управление учебными работами (ЛР, ПР) |
| `/api/pages`              | `practical-pages.json`        | Управление практическими страницами   |
| `/api/courseworks`        | `courseworks.json`            | Управление курсовыми работами         |
| `/api/essays`             | `essays.json`                 | Управление рефератами и эссе          |
| `/api/drafts`             | `drafts.json`                 | Управление черновиками                |
| `/api/compensatory-works` | `compensatory-works.json`     | Управление отработками                |

_Каждый ресурс поддерживает стандартные методы: `GET` (список), `GET /{id}` (один элемент), `POST` (создание), `PUT /{id}` (полное обновление), `DELETE /{id}` (удаление)._

---

## Загрузка контента ресурсов

#### PDF (для работ, курсовых и т.д.)

`POST /api/<resource_name>/{id}`

- **Пример:** `POST /api/labs/lab-1`
- **Тип:** `multipart/form-data`
- **Поле:** `file`
- Сервер автоматически определяет тип ресурса, сохраняет файл в соответствующую директорию (`/files/labs/`, `/files/courseworks/` и т.д.) и обновляет `url` и `fileName` в JSON-модели.

#### ZIP (архив с файлами сайта)

`POST /api/sites/{id}`

- **Тип:** `multipart/form-data`
- **Поле:** `file`
- Этот эндпоинт используется **только для загрузки архива с кодом сайта** (HTML, CSS, JS).
- Сервер распаковывает архив в директорию `/files/sites/{slug}/`.
- **Референсы (изображения-примеры) управляются отдельно.**

---

## Управление референсами сайта

Референсы — это изображения-примеры для страниц сайта. Они загружаются, обновляются и удаляются отдельно от основного архива сайта через специальные эндпоинты.

#### Добавление или замена референса

`POST /api/sites/{id}/references`
`PUT /api/sites/{id}/references/{fileName}`

- **Тип:** `multipart/form-data`
- **Поле:** `file` или несколько полей `file[]`
- **Метод `POST`** используется для добавления одного или нескольких новых референсов.
- **Метод `PUT`** используется для замены существующего референса с указанным `fileName`.
- Сервер сохраняет файлы в `/files/references/sites/{slug}/` и добавляет/обновляет соответствующие записи в массиве `references` в `sites.json`.

#### Удаление референса

`DELETE /api/sites/{id}/references/{fileName}`

- Удаляет указанный файл референса с сервера и соответствующую запись из `sites.json`.
- В качестве `{fileName}` используется имя файла, например, `header.png`.

---

## Коды ошибок

| Код | Описание                  |
| --- | ------------------------- |
| 400 | Неверный запрос           |
| 401 | Ошибка авторизации        |
| 403 | Доступ запрещён           |
| 404 | Ресурс не найден          |
| 409 | Конфликт данных           |
| 500 | Внутренняя ошибка сервера |
---
# Данные и модели

Все логические модели данных хранятся в JSON-файлах в директории `/data`. Каждая запись в файле имеет уникальное поле `id`, которое генерируется и используется только сервером.

## Пример: `sites.json`

Эта модель описывает сложный ресурс "сайт", который включает в себя вложенные страницы и ссылки на референсы.

```json
{
  "id": "site-1",
  "name": "Landing Project",
  "slug": "landing-project",
  "repositoryUrl": "https://github.com/example/repo",
  "figmaUrl": "https://www.figma.com/file/xxx",
  "referenceColumns": ["main", "mobile"],
  "pages": [
    {
      "id": "site-page-1",
      "name": "Главная",
      "url": "landing-project/index.html",
      "references": [
        "references/sites/landing-project/header.png",
        "references/sites/landing-project/footer.png"
      ]
    }
  ]
}
```

## Пример: `labs.json`

Эта модель описывает простой ресурс "лабораторная работа", который ссылается на один PDF-файл.

```json
{
  "id": "lab-1",
  "number": 1,
  "fileName": "ЛР1_Иванов_И.И.pdf",
  "url": "labs/ЛР1_Иванов_И.И.pdf"
}
```

---
# Ограничения и допущения

Этот документ описывает ключевые ограничения, принятые в рамках проекта.

## Технические ограничения
- **Язык и окружение**: Только чистый PHP 8.3, без использования пакетного менеджера Composer.
- **Хранение данных**: Данные хранятся исключительно в JSON-файлах. Базы данных (SQL, NoSQL) не используются.
- **Пользователи**: Система рассчитана на одного пользователя (администратора) с доступом по единому паролю.
- **Состояние**: API работает без сохранения состояния между запросами (stateless).
- **Логирование**: Механизмы логирования ошибок и действий отсутствуют.

## Архитектурные запреты
- **Фреймворки**: Запрещено использовать любые PHP-фреймворки (Laravel, Symfony, Slim, Yii и т.д.).
- **ORM**: Запрещено внедрять любые библиотеки для объектно-реляционного отображения (ORM).
- **Конфигурация**: Запрещено изменять конфигурационные файлы (`config.json`, `auth.php`) через API.
- **Прямой доступ к файлам**: Логика API не должна оперировать прямыми путями к файлам; все пути формируются на основе семантических правил и конфигурации.

## Безопасность
- **Аутентификация**: Используется только Basic Auth. Пароль на сервере хранится в виде хеша.
- **Доступ**: API предназначен для использования только из доверенной среды (React Admin Panel). CORS-политики или другие механизмы защиты для публичного доступа не предусмотрены.
- **Типы файлов**: Разрешена загрузка только PDF-файлов и ZIP-архивов. Выполнение кода из загруженных файлов исключено.
- **Валидация**: Все входящие данные должны проходить валидацию по JSON-схемам для предотвращения некорректных данных.
